import {TennisGame}  from "../src";

export const playerOneWin : TennisGame = {getScore(): string { return 'Win for' +
        ' player1'}, wonPoint(playerName: string) {
    }}

export const playerTwoWin : TennisGame = {getScore(): string { return 'Win for' +
        ' player2'}, wonPoint(playerName: string) {
    }}